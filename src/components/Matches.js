import React from 'react';

const Matches = () => {
  console.log('Rendering Matches component'); // Логируем факт рендеринга компонента
  return (
    <div>
      <h2>Upcoming Matches</h2>
      <p>Information about upcoming matches or recent results can be displayed here.</p>
    </div>
  );
};

export default Matches;